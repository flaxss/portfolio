# ─── Development image — Vite HMR, no build step ────────────────────────────
FROM node:20-alpine

WORKDIR /app

# Install deps
COPY package.json ./
RUN npm install

# Source is mounted as a volume at runtime — no COPY needed
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
